import { InputBoxConfigDirective } from './input-box-config.directive';

describe('InputBoxConfigDirective', () => {
  it('should create an instance', () => {
    const directive = new InputBoxConfigDirective();
    expect(directive).toBeTruthy();
  });
});
